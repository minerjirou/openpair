# セキュリティポリシー

*English: [SECURITY.md](SECURITY.md)*

openpair は暗号ペアリングと mutual TLS 信頼を実装するため、セキュリティ報告を重視します。

## 脆弱性の報告

**脆弱性を公開 issue に書かないでください。**

代わりに、本リポジトリの GitHub 非公開の **"Report a vulnerability"**（Security Advisories）
を使うか、メンテナに私的に連絡してください。以下を含めてください：

- 影響するコンポーネント / バージョン（コミットハッシュ）
- 説明と影響
- 再現手順または PoC

報告には速やかに応答し、修正と開示を調整します。

## スコープに関する注記

- ペアリング（EAP-NOOB）実装には `TODO(interop)` の byte 厳密シリアライズが残っています。
  確定するまで、クロスベンダーのペアリングをセキュリティ境界として依存しないでください。
- 信頼は TLS 1.3 相互認証上の pin 済み証明書 DER によります。dev-trust ディレクトリ
  （`OPENPAIR_TRUST_DIR`）を信頼できない場所に向けないでください。
- 本ソフトウェアは初期段階であり、Apache-2.0 の下で**無保証**で提供されます。本番利用前に
  レビューしてください。
